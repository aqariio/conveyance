# Gradle Properties
org.gradle.jvmargs = -Xmx2G
org.gradle.parallel = true

# Mod Properties
version = 0.3.2-alpha
maven_group = aqario.conveyance
archives_base_name = conveyance

# Dependencies are managed at gradle/libs.versions.toml
